const Semititle  = ({children}) => {
    return  (
    <div>
        <p style = {{
            fontSize:"30px",
            fontFamily:"continuous"  
        }}>{children}</p>
    </div>
)}

export default Semititle 