const Semititle  = ({children}) => {
    return  (
    <div>
        <p style = {{
            fontSize:"15px",
            fontFamily:"MaruBuriLight"  
        }}>{children}</p>
    </div>
)}

export default Semititle 